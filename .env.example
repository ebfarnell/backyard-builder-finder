# Supabase Configuration (Required)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
# Service role key for server-side operations (if using edge functions)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Map Configuration
# MapTiler API key for basemap tiles (optional, will fallback to open style)
VITE_MAPTILER_API_KEY=your-maptiler-api-key

# Geocoding Provider (default: nominatim)
VITE_GEOCODER_PROVIDER=nominatim
# VITE_GEOCODER_PROVIDER=maptiler
# VITE_GEOCODER_PROVIDER=opencage

# Geocoding API keys (optional, depending on provider)
VITE_OPENCAGE_API_KEY=your-opencage-api-key

# Parcel Data Providers (optional)
# Regrid API for parcel boundary data
VITE_REGRID_API_KEY=your-regrid-api-key

# ArcGIS Feature Server URL for parcel data (region-specific)
VITE_ARCGIS_FEATURESERVER_URL=https://services.arcgis.com/your-service-url

# Application Configuration
VITE_APP_URL=http://localhost:3000
VITE_BASE_PATH=/
VITE_DEFAULT_ZONING_CODE=R1

# Development
NODE_ENV=development